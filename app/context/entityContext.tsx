"use client";

import React, { createContext, useContext } from 'react'
import {createContext,useContext} from 'react'

const entityReducerState ={
    
}
function entityContext() {
  return (
    <div>entityContext</div>
  )
}

export default entityContext